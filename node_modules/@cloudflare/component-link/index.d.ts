import * as React from 'react';
import { TfelaMProps } from '@cloudflare/types';

interface ILinkProps {
  to?: string;
  href?: string;
  onClick?: Function;
  onFocus?: Function;
  onBlur?: Function;
  disabled?: boolean;
  role?: string;
  expandable?: boolean;
  isActive?: boolean;
  title?: string;
  className?: string;
  testId?: string;
  style?: React.CSSProperties;
}
export class Link extends React.PureComponent<ILinkProps & TfelaMProps> {}
export const LinkTheme: object;
