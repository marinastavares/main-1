// this is a wrapper for testing, it passes fela based
// components fela's renderer and global theme
import React from 'react';
import StyleProvider from './StyleProvider';

var felaTestContext = (component, renderer, theme) => /*#__PURE__*/React.createElement(StyleProvider, {
  renderer: renderer,
  dev: true,
  theme: theme
}, component);

export default felaTestContext;