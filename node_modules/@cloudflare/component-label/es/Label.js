import PropTypes from 'prop-types';
import { createStyledComponent } from '@cloudflare/style-container';
var typeColors = {
  default: 'gray.4',
  info: 'blue.4',
  success: 'green.4',
  warning: 'orange.5',
  error: 'red.4'
};

var styles = function styles(_ref) {
  var theme = _ref.theme,
      type = _ref.type;
  return {
    alignItems: 'center',
    borderRadius: theme.radii[2],
    color: theme.colors.white,
    display: 'inline-flex',
    fontSize: 1,
    fontWeight: 600,
    lineHeight: theme.lineHeights.solid,
    userSelect: 'none',
    whiteSpace: 'nowrap',
    px: 2,
    py: 1,
    backgroundColor: typeColors[type]
  };
};

var Label = createStyledComponent(styles, 'span');
Label.propTypes = {
  type: PropTypes.oneOf(['default', 'info', 'success', 'warning', 'error']).isRequired
};
Label.displayName = 'Label';
export default Label;