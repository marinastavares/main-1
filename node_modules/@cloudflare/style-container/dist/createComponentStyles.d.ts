import { ComponentType } from 'react';
import { StyleFunctions } from './types';
export declare function createComponentStyles<Styles extends StyleFunctions, ComponentProps extends {
    styles: {
        [style in keyof Styles]: string;
    };
}>(styleFunctions: Styles, component: ComponentType<ComponentProps>): ComponentType<Omit<ComponentProps, 'styles'>>;
export default createComponentStyles;
