"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.addDisplayName = exports.hocDisplayName = void 0;

var _reactDisplayName = _interopRequireDefault(require("react-display-name"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// create displayName function for a component (WrappedComponent) which is wrapped
// by a HOC.
// hocType can be either a string (such as 'div') or a React Component
var hocDisplayName = function hocDisplayName(hocType) {
  return function (WrappedComponent) {
    var displayName = '';

    try {
      displayName = (0, _reactDisplayName.default)(WrappedComponent);
    } catch (ex) {
      if (process.env.NODE_ENV !== 'production') {
        console.error("can't get displayName, exception: ".concat(ex));
      }
    }

    return "".concat(hocType, "(").concat(displayName, ")");
  };
}; // Add displayName to a Component
// In addition adds setDisplayName function for setting proper display after HOC is created.


exports.hocDisplayName = hocDisplayName;

var addDisplayName = function addDisplayName(HOC, WrappedComponent, getDisplayName) {
  HOC.displayName = getDisplayName(WrappedComponent);

  HOC.setDisplayName = function (displayName) {
    HOC.displayName = getDisplayName(displayName);
  };

  return HOC;
};

exports.addDisplayName = addDisplayName;