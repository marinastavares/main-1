import { createComponent } from '@cloudflare/style-container';

const NavList = createComponent(
  ({ theme }) => ({
    display: 'none',
    lineHeight: '42px',
    textAlign: 'right',
    margin: 0,
    padding: 0,
    marginRight: theme.space[4],
    listStyle: 'none',
    tablet: {
      display: 'block'
    }
  }),
  'ul'
);
NavList.setDisplayName('NavList');

export default NavList;
